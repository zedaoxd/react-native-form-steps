import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
  pageTitle: {
    fontSize: 24,
    fontWeight: "bold",
    marginBottom: 20,
  },
  titleSection: {
    fontSize: 18,
    fontWeight: "bold",
    marginTop: 20,
  },
  bold: {
    fontWeight: "bold",
  },
});
